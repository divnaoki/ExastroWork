# Hyper-V上仮想マシンの自動構築および設定 — 設計書

- 生成日: 2026-06-01
- 対象: Exastro IT Automation v2.x
- ドライバ: **Ansible Legacy Role**（1ロール = 1 Movement）
- スコープ: **設計のみ**。Exastroへの登録・インポートは行わない。本書を見ながら手動登録する。

---

## 1. 案件概要
| 項目 | 内容 |
|------|------|
| 案件名 | Hyper-V上仮想マシンの自動構築および設定 |
| 目的・背景 | Hyper-V上に RHEL9.6 と WindowsServer2022 の仮想マシンを作成し、設定値を投入する |
| 作業種別 | ①VMプロビジョニング（テンプレートインポート〜起動）＋ ②OS初期設定（静的IP・時刻同期） |
| タイミング・頻度 | 初回構築のみ |

### 対象環境
| 項目 | 内容 |
|------|------|
| 実行対象 | **Hyper-Vホスト 4台 / WinRM接続**（ゲスト操作もホスト経由） |
| VM世代 | Generation 2（RHEL/Windows 両方） |
| 作成VM | RHEL9.6 × 1、WindowsServer2022 × 約10 |
| 作成方式 | エクスポート済みテンプレートVMを `Import-VM`（ISOマウント不要） |
| RHEL設定 | kickstartでIP割当済 → 起動＋時刻同期のみ |
| Windows設定 | PowerShell Direct で静的IP投入＋時刻同期 |
| 時刻同期 | 全VMで Time Synchronization 統合サービス有効化（Hyper-Vホスト時刻に同期） |

---

## 2. Conductor設計 / ロールパッケージ
**ロールパッケージを3分割**し、Conductorも3本に対応させる。HW設定は**逐次**、失敗時は**即時中断**。図は `conductor/` 配下（`.mmd`/`.md`/`.html`）。

| パッケージ | Conductor | 実行対象 / 接続 | 含むロール |
|-----------|-----------|------------------|-----------|
| **vm_build** | A「仮想マシン構築」 | Hyper-Vホスト / WinRM | import_template_vm, set_vm_cpu, set_vm_memory, configure_vm_network, configure_vm_firmware, enable_time_sync, start_vm |
| **windows_config** | B「Windows設定変更」 | Hyper-Vホスト / WinRM（PowerShell Direct） | configure_windows_ip |
| **rhel_config** | C「RHEL設定変更」 | RHELゲスト / **SSH**（スケルトン） | configure_rhel_settings |

### Conductor A「仮想マシン構築」(pkg: vm_build)
```
import_template_vm → set_vm_cpu → set_vm_memory → configure_vm_network
  → configure_vm_firmware → enable_time_sync → start_vm
```
根拠: HW構成（CPU/メモリ/NW/ファームウェア）はVM**停止中**に行う必要があるため、すべて `start_vm` より前。時刻同期（統合サービス）も起動前に有効化。

### Conductor B「Windows設定変更」(pkg: windows_config)
```
configure_windows_ip （when os_type == windows）
```
根拠: IP投入はVM**起動後**に行うためConductorを分離。実行対象はHyper-Vホスト（PowerShell Direct）。

### Conductor C「RHEL設定変更」(pkg: rhel_config) ※スケルトン
```
configure_rhel_settings （RHELゲストにSSH接続）
```
根拠: RHEL設定はゲスト到達後に実施。Windowsと接続方式（SSH vs WinRM/PowerShell Direct）が異なるためパッケージ・Conductorを分離。現状RHEL固有の設定値は未確定のためスケルトン。

---

## 3. Movement一覧（9ロール / 3パッケージ）
| # | Movement/ロール | パッケージ | Conductor | 対象 | WinRM |
|---|-----------------|-----------|-----------|------|-------|
| 1 | import_template_vm | vm_build | A | 全VM | True |
| 2 | set_vm_cpu | vm_build | A | 全VM | True |
| 3 | set_vm_memory | vm_build | A | 全VM | True |
| 4 | configure_vm_network | vm_build | A | 全VM | True |
| 5 | configure_vm_firmware | vm_build | A | 全VM | True |
| 6 | enable_time_sync | vm_build | A | 全VM | True |
| 7 | start_vm | vm_build | A | 全VM | True |
| 8 | configure_windows_ip | windows_config | B | Windowsのみ | True |
| 9 | configure_rhel_settings ※雛形 | rhel_config | C | RHELゲスト | **False(SSH)** |

各Movementの登録項目（オーケストレータ/ホスト指定形式/ヘッダーセクション等）は `movements/` 配下を参照。
- vm_build / windows_config のヘッダーセクション: `- hosts: all` / `gather_facts: false`（WinRMのため `become` 省略）。
- rhel_config のヘッダーセクション: `- hosts: all` / `gather_facts: true` / `become: true`（SSH接続）。

---

## 4. Playbook設計（冪等性・前後取得）
- 全ロールが `packages/<pkg>/roles/<名>/tasks/main.yml` → `tasks/<名>.yml` を `include_tasks` する標準構造。
- 既定値は `defaults/main.yml`（`VAR_vm: []`、構造コメントは import_template_vm に記載）。
- 共通構成: **事前取得(before) → 変更(冪等) → 事後取得(after) → エビデンス出力(debug)**。
- **Hyper-V操作は `microsoft.hyperv` コレクションの冪等モジュールを第一選択**（CPU/メモリ/NW/ブート/電源）。
  モジュールが存在しない操作（テンプレートImport・統合サービス）のみ `ansible.windows.win_shell`＋PowerShellで実装し、
  `when` ガード＋`changed_when` で冪等化する。before/after の読み取りは read-only な `win_shell`（`changed_when: false`）。
- 各ロールの実装モジュールと冪等性:
  | ロール | 使用モジュール | 冪等性 |
  |--------|----------------|--------|
  | import_template_vm | `win_shell`（Import-VM ※モジュール無し） | `Get-VM` 同名存在 → 無い場合のみImport |
  | set_vm_cpu | **`microsoft.hyperv.hv_processor`** | モジュールが count に収束 |
  | set_vm_memory | **`microsoft.hyperv.hv_memory`** | startup/min/max/dynamic に収束 |
  | configure_vm_network | **`microsoft.hyperv.hv_network_adapter`** | switch_name/vlan に収束 |
  | configure_vm_firmware | **`microsoft.hyperv.hv_vm_boot`** | secure_boot/template に収束（Gen2） |
  | enable_time_sync | `win_shell`（統合サービス ※モジュール無し） | `Get-VMIntegrationService` Enabled比較 |
  | start_vm | **`microsoft.hyperv.hv_vm_state`** | state: running（起動後 `until` 待機） |
  | configure_windows_ip | `win_shell`（PowerShell Direct ※ゲスト内操作） | ゲスト内現IP比較 |
- **モジュールマニュアル運用**: 使用する `microsoft.hyperv.*` は Obsidian `Ansible_Docs/` に登録済み。
  `hv_vm_boot` は本対応で `ansible-doc` から取得し日本語マニュアルを新規登録した。
- `configure_vm_firmware` の `item.secure_boot` マッピング: `on`→secure_boot:true+template:MicrosoftWindows /
  `off`→secure_boot:false / `rhel`→secure_boot:true+template:MicrosoftUEFICertificateAuthority。
  | configure_rhel_settings | ※スケルトン（設定値確定後に冪等タスクを実装） |

---

## 5. パラメータシート設計
- メニュー名: **VM構築パラメータ** / **バンドル設定 ON**（1レコード = 1VM）。
- 縦軸ホスト = Hyper-Vホスト。各ホストに、そのホストで作成するVMの数だけレコードを登録。
- 17項目（複数具体値変数 `VAR_vm` のメンバー）。詳細は `parameter_sheets/VM構築パラメータ.md`。
- 機密: `guest_admin_password`（no_log）。

---

## 6. 自動代入値設定
- 複数具体値変数 `VAR_vm`、メンバー17。**代入順序 = loop順**。全メンバーで順序を揃える。
- 詳細・対応表は `assignment/自動代入値設定.md`。

---

## 7. オペレーション・実行対象
| オペレーション | Conductor / pkg | 実行対象 |
|----------------|-----------------|----------|
| Hyper-V_VM構築_初回 | A / vm_build | Hyper-Vホスト 4台 |
| Hyper-V_Windows設定_初回 | B / windows_config | Hyper-Vホスト 4台 |
| RHEL設定_初回 ※雛形 | C / rhel_config | RHELゲストVM（SSH） |
- 初回構築のみのため各1オペレーション。A/Bはホスト個別値＋バンドルでVM差分を吸収。
- Cは実行対象がRHELゲストのため、別途RHELゲストの機器一覧登録（と必要なら専用パラメータシート）が要る。

---

## 8. エビデンス方針
- 各ロールで before → after を `debug` 出力（差分明示）。
- `guest_admin_password` を扱うタスクは `no_log: true`（IPエビデンスにパスワードを含めない）。
- 起動はRunning到達まで `until`（retries:30/delay:10）で待機。

---

## 9. 未確定事項・前提
1. **テンプレートパスの実値**: 未提示のため `VAR_vm.template_path` でパラメータ化。値は**エクスポートVMの`Virtual Machines`フォルダ**（.vmcxの1つ上）を登録し、配下の`*.vmcx`を `win_find` で取得して `Import-VM` に渡す。RHEL用/Windows用で別パス。
2. **VM配置（どのVMをどのHyper-Vホストへ）**: パラメータシートのホスト個別値＋バンドルで表現（各Hyper-Vホスト行にそのホストで作るVMを登録）。具体の割当は登録時に確定。
3. **ゲスト管理者資格情報**: テンプレート組込の共通Administrator想定。`no_log` 管理。実値は登録時に投入。
4. **OS設定値**: 現時点はWindows静的IP・時刻同期のみ。Windows追加設定は windows_config に、RHEL設定は **rhel_config（現状スケルトン）** に追記して拡張。
5. **RHEL設定変更パッケージ（rhel_config）**: RHEL固有のOS設定値が未確定のためスケルトン。実行対象は**RHELゲスト（SSH）**で他2パッケージ（Hyper-Vホスト/WinRM）と接続方式が異なる。設定値が決まり次第、機器一覧へのRHELゲスト登録・専用パラメータシート・冪等タスクを実装する。
6. **PowerShell Direct前提**: Hyper-V統合サービス（ゲストサービス）が有効でゲストOSが応答可能であること。VM起動後に十分な初期化時間が必要な場合は configure_windows_ip 前に待機を追加検討。
7. **モジュール**: Hyper-V操作は `win_shell`＋PowerShellで実装。専用モジュール化が必要な場合は別途検討（Obsidian `Ansible_Docs/` 運用に従う）。

---

## 付録: 出力ファイル構成
```
hyperv_vm_provisioning_20260601/
├── 00_設計書.md
├── conductor/
│   ├── conductor_A_vm_build.mmd / .md / .html
│   ├── conductor_B_windows_config.mmd / .md / .html
│   └── conductor_C_rhel_config.mmd / .md / .html
├── movements/ (01〜09)
├── packages/                          # ロールパッケージ（3分割）
│   ├── vm_build/roles/
│   │   ├── import_template_vm/  (tasks/main.yml, tasks/import_template_vm.yml, defaults/main.yml)
│   │   ├── set_vm_cpu/
│   │   ├── set_vm_memory/
│   │   ├── configure_vm_network/
│   │   ├── configure_vm_firmware/
│   │   ├── enable_time_sync/
│   │   └── start_vm/
│   ├── windows_config/roles/
│   │   └── configure_windows_ip/
│   └── rhel_config/roles/
│       └── configure_rhel_settings/   # スケルトン（RHEL設定値確定後に実装）
├── parameter_sheets/VM構築パラメータ.md
└── assignment/自動代入値設定.md
```
